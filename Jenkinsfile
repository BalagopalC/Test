pipeline {
 agent {
    node {
      label 'master'
      customWorkspace 'd:\\jenkinTest'
    }
  }
   stages {
    stage('NuGet Restore') {
      steps {
       echo 'hello'
      
      }
    }
   }
}
