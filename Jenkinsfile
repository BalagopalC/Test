pipeline {
 agent {
    node {
      label 'master'
      customWorkspace 'c:\\jenkinTest'
    }
  }
   stages {
    stage('NuGet Restore') {
      steps {
       echo 'hello'
      
      }
    }
   }
}
